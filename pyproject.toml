[project]
name = "jsonplaceholder-sdk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.33.0",
]

[tool.ruff.lint.isort]
force-single-line = true
